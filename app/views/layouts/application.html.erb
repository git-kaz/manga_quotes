<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "プレゼンテーションに一言を" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <!-- 動的OGP -->
    <meta property="og:title" content="<%= content_for?(:og_title) ? yield(:og_title) : 'プレゼンテーションに一言を' %>">
    <meta property="og:description" content="<%= content_for?(:og_description) ? yield(:og_description) : '心に響く名言やセリフを見つけて、プレゼンをもっと魅力的に！' %>">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:image" content="https://manga-quotes-oe1x.onrender.com/assets/images/share.png">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">   
    <!-- twitter card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= content_for?(:twitter_title) ? yield(:twitter_title) : 'プレゼンテーションに一言を' %>">
    <meta name="twitter:description" content="<%= content_for?(:twitter_description) ? yield(:twitter_description) : '心に響く名言やセリフを見つけて、プレゼンをもっと魅力的に！' %>">
    <meta name="twitter:image" content="https://manga-quotes-oe1x.onrender.com/assets/images/share.png">
    
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
  </head>

  <body class="min-h-screen bg-background-light">
    <% unless devise_controller? %>
      <header class="bg-white shadow-sm fixed top-0 left-0 right-0 z-10">
        <nav class="container mx-auto px-4 py-4">
          <div class="flex items-center justify-between">
            <!-- 左側: ロゴ/タイトル -->
            <div class="flex items-center">
            <span class="material-symbols-outlined text-[20px] text-primary mx-4">auto_stories</span>
              <%= link_to root_path, class: "text-2xl font-bold text-[#1b140d] hover:text-primary transition-colors" do %>
                プレゼンテーションに一言を
              <% end %>
            </div>

            <!-- 右側: ナビゲーションメニュー -->
            <ul class="flex items-center gap-4">
              <%= render 'search', q: @q, url: quotes_path %>
            <% if user_signed_in? %>

              <li class="relative" data-controller="dropdown" data-action="click@window->dropdown#hide">
                <div class="flex ml-4">
                <span class="material-symbols-outlined mr-2">identity_platform</span>
                <button
                  data-action="click->dropdown#toggle"
                  class="hover:text-[#d67b20] transition-colors font-medium cursor-pointer flex items-center gap-2"
                >
                  <%= current_user.username %>
                  <span class="material-symbols-outlined text-base">arrow_drop_down_circle</span>
                </button>
                </div>
                <!--ドロップダウン -->
                <div
                  data-dropdown-target="menu"
                  class="hidden absolute right-0 mt-2 px-4 w-48 bg-white rounded-lg shadow-lg py-2 z-10"
                >
                  <%= link_to "マイページ", edit_user_registration_path, 
                  class: "text-primary hover:text-[#d67b20] transition-colors font-medium cursor-pointer" 
                  %>
                  <%= button_to "ログアウト", destroy_user_session_path, 
                  method: :delete, 
                  class: "text-primary hover:text-[#d67b20] transition-colors font-medium cursor-pointer" 
                  %>
                </div>
              </li>
            <% else %>
              <li>
                <%= link_to "新規登録", new_user_registration_path, 
                  class: "text-primary hover:text-[#d67b20] transition-colors font-medium cursor-pointer" 
                %>
              </li>
              <li>
                <%= link_to "ログイン", new_user_session_path, 
                  class: "text-white bg-primary hover:bg-orange-600 transition-colors font-medium p-2 rounded-lg cursor-pointer"  
                %>
              </li>
            <% end %>
          </ul>
        </nav>

      </header>
    <% end %>

    <main class="<%= devise_controller? ? '' : 'container mx-auto mt-28 px-5' %>">
      <%= yield %>
    </main>
<footer class="mt-auto border-t border-border-color bg-white py-8">
<div class="mx-auto flex max-w-7xl flex-col items-center justify-end gap-4 px-4 sm:flex-row sm:px-6 text-sm text-text-secondary">
<p>© 2025 MangaDialogue. All rights reserved.</p>
</div>
</footer>
  </body>
</html>

